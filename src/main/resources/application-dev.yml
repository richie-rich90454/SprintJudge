openquiz:
  executor:
    # Native toolchains by default so a fresh Windows box tests the full OJ
    # flow with zero WSL setup. Set OPENQUIZ_EXECUTOR_MODE=wsl for isolation.
    mode: ${OPENQUIZ_EXECUTOR_MODE:native}
  db:
    path: ./openquiz.db
logging:
  level:
    com.openquiz: DEBUG
